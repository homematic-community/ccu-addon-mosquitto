# Task 9 — Documentation ✅

Completed 2026-09-05.

- `README.md` rewritten (German, English summary): what the addon is,
  the three packages and platforms, install and CCU3 firewall hint, the
  default configuration, migration from 1.5.8, the configuration page
  card by card (incl. bridges and the firewall status), command line
  (config file, `rc.d`, clients, `mosquitto_passwd`, `mosquitto_ctrl`),
  the Mosquitto 2.x `allow_anonymous` note, update, version scheme,
  development pointers, licenses.
- `docs/RELEASE_NOTES.md` for the first release: Mosquitto 2.1 instead of
  1.5.8, three platforms, configuration page and self-update, the
  `conf.d` migration, `allow_anonymous`, version scheme, requirements.
- `BUILD.md`: pipeline, requirements, tests, release workflows, manual
  installation over ssh.
- `AGENTS.md` for future sessions.
- No wiki for this addon; the README carries the user documentation.
